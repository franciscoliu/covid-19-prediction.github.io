        // chart4.options.data[0].dataPoints = [];
        // for (let i = 0; i < data.length; i++) {
        //     dataPoints1.push({
        //         x: new Date(Date.parse(data[i].Date)),
        //         y: data[i]["Confirmed Cases"]
        //     }); 
        //     chart4.options.title.text = "COVID-19 Infection Trend" + "(" + document.getElementById('searchbox').value + ")";
        //     chart4.options.data[0].dataPoints[i-1] = {x: new Date(Date.parse(data[i].Date)), y:data[i]["Confirmed Cases"]};
        // }
        // chart4.render();
        // chart4.options.data[1].dataPoints = [];
        // for (let i = 0; i < data.length; i++) {     
        //     dataPoints2.push({
        //         x: new Date(Date.parse(data[i].Date)),
        //         y: data[i]["Predicted Cases"]
        //     })
        //     chart4.options.data[1].dataPoints[i-1] = {x: new Date(Date.parse(data[i].Date)), y:data[i]["Predicted Cases"]}
        // }   ///搜索框里的value引用这个函数，之后改data里面的值
        // chart4.render();

        // chart4.options.data[1].dataPoints = [];
        // for (let i = 0; i < data.length; i++) {     
        //     dataPoints5.push({
        //         x: new Date(Date.parse(data[i].Date)),
        //         y: data[i]["Predicted Cases"]
        //     })
        //     chart4.options.data[1].dataPoints[i-1] = {x: new Date(Date.parse(data[i].Date)), y:data[i]["Predicted Cases"]}
        // }   ///搜索框里的value引用这个函数，之后改data里面的值
        // chart4.render();

        // chart4.options.data[1].dataPoints = [];
        // for (let i = 0; i < data.length; i++) {     
        //     dataPoints6.push({
        //         x: new Date(Date.parse(data[i].Date)),
        //         y: data[i]["Predicted Cases"]
        //     })
        //     chart4.options.data[1].dataPoints[i-1] = {x: new Date(Date.parse(data[i].Date)), y:data[i]["Predicted Cases"]}
        // }   ///搜索框里的value引用这个函数，之后改data里面的值
        // chart4.render();